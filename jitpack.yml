jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.10-zulu || true

install:
  - ./gradlew :gradle-plugin:publishToMavenLocal :processor:publishToMavenLocal :core:publishToMavenLocal -Pbootstrap=true
